# Copie para .env.local (dev) — nunca commitar valores reais.

# URL da API Fastify (backend). Em dev, o backend roda em :3001.
API_URL="http://localhost:3001"

# DEVE ser IDÊNTICO ao INTERNAL_API_SECRET do backend (a barreira HMAC).
INTERNAL_API_SECRET="troque-por-um-segredo-forte-igual-ao-backend"
